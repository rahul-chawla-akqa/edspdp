# SUPERSEDED -- this file is no longer the source of truth for the content mountpoint.
#
# fstab.yaml cannot express a BYOM content overlay, so the mountpoint below was migrated into a
# site config held by the configuration service, which now also carries the overlay pointing at
# the server-side composer. That config has been confirmed working on a composed preview, so the
# values here are ignored and are kept only as a record of what was migrated. Inspect the live
# configuration instead:
#
#   npm run ssr:wire -- check    # primary source and overlay actually in effect
#   npm run ssr:wire -- status   # the full site config
#
# See docs/ssr-overlay-architecture.md
mountpoints:
  /:
    url: "https://author-p104103-e1884364.adobeaemcloud.com/bin/franklin.delivery/rahul-chawla-akqa/edspdp/main"
    type: "markup"
    suffix: ".html"
